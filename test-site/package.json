{
  "name": "reservation-monitor-test-site",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute reservation-monitor-test --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "latest"
  }
}
